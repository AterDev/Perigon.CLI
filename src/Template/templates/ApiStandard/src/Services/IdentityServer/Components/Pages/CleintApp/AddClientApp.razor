@inherits PageBase
@implements IDialogContentComponent

<FluentDialogHeader ShowDismiss="false">
    <FluentStack VerticalAlignment="VerticalAlignment.Center">
        <FluentLabel Typo="Typography.PaneHeader">
            @Dialog.Instance.Parameters.Title
        </FluentLabel>
    </FluentStack>
</FluentDialogHeader>

<FluentDialogBody>
    <EditForm Model="@AddDto">
        <FluentStack Orientation="Orientation.Vertical">
            <FluentTextField @bind-Value="AddDto.ClientName" Placeholder="@Localizer.Get(LanguageKey.ClientName)" class="w-100" Required />
            <FluentValidationMessage For="() => AddDto.ClientName"></FluentValidationMessage>

            <FluentTextArea Rows="3" @bind-Value="RedirectUris" Placeholder="@Localizer.Get(LanguageKey.RedirectUris)" Class="w-100" Required />
            <FluentLabel Typo="Typography.Body" Color="Color.Info">
                @Localizer.Get(LanguageKey.RowValueTip)
            </FluentLabel>
            <FluentValidationMessage For="() => AddDto.RedirectUris"></FluentValidationMessage>
        </FluentStack>
    </EditForm>
</FluentDialogBody>

<FluentDialogFooter>
    <FluentButton Appearance="Appearance.Accent" OnClick="@SaveAsync">
        @Localizer.Get(LanguageKey.Save)
    </FluentButton>
    <FluentButton Appearance="Appearance.Neutral" OnClick="@CancelAsync">
        @Localizer.Get(LanguageKey.Cancel)
    </FluentButton>
</FluentDialogFooter>

@code {
    public ClientAppAddDto AddDto { get; set; } = new();

    public string RedirectUris { get; set; } = string.Empty;

    [CascadingParameter]
    public FluentDialog Dialog { get; set; } = default!;

    [Inject]
    private ApplicationManager ApplicationManager { get; set; } = default!;

    private async Task SaveAsync()
    {
        if (string.IsNullOrWhiteSpace(AddDto.ClientName) || string.IsNullOrWhiteSpace(RedirectUris))
        {
            return;
        }
        if (!string.IsNullOrEmpty(RedirectUris))
        {
            var separators = new[] { "\r\n", "\n" };
            AddDto.RedirectUris = RedirectUris.Split(separators, StringSplitOptions.RemoveEmptyEntries | StringSplitOptions.TrimEntries)
                                              .ToList();
        }

        var res = await ApplicationManager.CreateAsync(AddDto);
        Console.WriteLine(ToJson(res));

        if (res is not null)
        {
            await Dialog.CloseAsync(true);
        }
        else
        {
            ToastService.ShowError(Lang(LanguageKey.Add, LanguageKey.Failed));
        }
    }

    private async Task CancelAsync()
    {
        await Dialog.CancelAsync();
    }
}
